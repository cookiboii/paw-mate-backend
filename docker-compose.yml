version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: qwer1234
      MYSQL_DATABASE: adoptpet_db
    volumes:
      - mysql-data:/var/lib/mysql
    networks:
      - adoptnet
    ports:
      - "3306:3306"

  redis:
    image: redis:7
    container_name: redis
    restart: always
    networks:
      - adoptnet
    ports:
      - "6379:6379"

  spring-backend:
    image: spring-backend  # 🔥 사전에 .tar로 전달해서 docker load로 등록됨
    container_name: spring-backend
    depends_on:
      - mysql
      - redis
    ports:
      - "80:8000"
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/adoptpet_db?useSSL=false&serverTimezone=Asia/Seoul
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: qwer1234
      SPRING_REDIS_HOST: redis
    networks:
      - adoptnet

volumes:
  mysql-data:

networks:
  adoptnet:
